git checkout -b metricai-config-personalizacao
git add -A
$Message = "feat(settings/dashboard): personalizacoes em PT-BR, novos campos e migração (extras + colunas dedicadas)"
git commit -m $Message
git push -u origin metricai-config-personalizacao
